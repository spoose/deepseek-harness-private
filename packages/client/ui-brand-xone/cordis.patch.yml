# xOne Web brand layer. Apply after dsh-web-app so the stable official row is
# retained but disabled before the xOne occupant is inserted.

- id: ui-brand-official
  disabled: true

- insert:
    - id: ui-brand-xone
      name: '@deepseek-ai/dsh-client-ui-brand-xone'
